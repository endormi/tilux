#include<stdio.h>
#include<stdlib.h>

int main()
{
    // (WIP) this for sure will be updated. I just wanted to push this right now
    system("inxi -C");
    system("inxi -G");
    system("inxi -D");
}
