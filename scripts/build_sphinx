#!/bin/bash

# TODO: Make better
[ ! -d scripts ] && cd ..
[ -d docs ] && cd docs

cmd=$(pip3 show sphinx)

if [ $? == 1 ]; then
  pip3 install sphinx
  clear
fi

cmd=$(pip3 show myst-parser)

if [ $? == 1 ]; then
  pip3 install myst-parser
  clear
fi

make html
